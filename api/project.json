{
  "name": "api",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "api",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "go build -o dist/api ./cmd/api",
        "cwd": "api"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "export CHROME_PATH=tools/flatpak-chrome-wrapper.sh && go build -o dist/api ./cmd/api && exec ./dist/api",
        "cwd": "api"
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "CHROME_PATH=tools/flatpak-chrome-wrapper.sh go tool wgo run ./cmd/api",
        "cwd": "api"
      }
    }
  }
}
