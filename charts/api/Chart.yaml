apiVersion: v2
name: tiny-dash-api
description: tiny-dash dashboard API — renders an e-ink dashboard via headless Chrome
type: application
home: https://github.com/thiemok/tiny-dash
sources:
  - https://github.com/thiemok/tiny-dash
# version  — the chart's own version (bumped by the release workflow alongside appVersion).
# appVersion — the api image version this chart deploys by default (kept in sync with the
#              api-v* release tag by the release workflow).
version: 0.2.0
appVersion: "0.2.0"
